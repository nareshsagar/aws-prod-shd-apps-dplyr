 <nav search_view="search" color="#960018" >
        < view name = "welcome" default = "true" / >
        < view name = "search" / >
        < view name = "datasets" / >
        < view name = "reports" / >
        < view name = "alerts" / >
        < view name = "atsiboards" / >
        < collection label = "Azure" >
        < view name = "azure health metrics" / >
        < view name = "azure_iac dashboard activity logs" / >
        < / collection >
        < collection label = "AWS" >
        < view name = "aws health_metrics" / >
        < view name = "aws iac dashboard cloudt rails logs" / >
        < / collection >
        </nav>
        